# Zotero-Cat 0.3.1 Automated Release Verification

[English](./0.3.1-automated.md) | [中文](./0.3.1-automated.zh-CN.md)

Date: 2026-05-23
Machine OS: macOS (local)
Node.js: v24.15.0
Package version: 0.3.1
Release tag target: `v0.3.1`

## Automated Checks

- `npm run lint:check`: passed.
- `npm run build`: passed.
- `npm test`: passed locally, 164 tests.

## Release Asset

- Release: <https://github.com/Zotero-Cat/Zotero-Cat/releases/tag/v0.3.1>
- Expected XPI asset: <https://github.com/Zotero-Cat/Zotero-Cat/releases/download/v0.3.1/zotero-cat-v0.3.1.xpi>
- XPI size: record after the tag workflow publishes the asset.
- XPI digest: record after the tag workflow publishes the asset.

## Manifest Compatibility

The generated packaged manifest contains:

```json
{
  "strict_min_version": "9.0",
  "strict_max_version": "9.*"
}
```

## Release State

- `v0.3.1` is intended as a non-draft, non-prerelease GitHub Release.
- `v0.3.1` should become the visible Latest release after the tag workflow
  publishes.
- The special `release` tag remains a pre-release updater-manifest release.

## Manual Gates

This automated note does not replace the Zotero GUI checklist. Keep
`doc/UI_REGRESSION_CHECKLIST.md` current for Zotero 9 manual validation, and do
not declare Zotero 10 compatibility until the current Zotero beta line passes
that checklist.
